# monitoring_networks
Optimize monitoring network of wells to minimize sampling and maximize information

Currently can:
- calculates statistics of the input data (wells of the monitoring network)
- can use well weights
- imports and can use grid with weights
- can prioritize multiple parameters with weights
- Shows the priorization order
- can show a map with the most-informative first N wells selected
